### Example environment variables for the project
### Copy this file to `.env.local` and fill values for your local environment.

# SMTP configuration (for sending lead emails)
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=you@example.com
SMTP_PASS=your-smtp-password-or-app-password
SMTP_FROM="Quiz Ateneo <you@example.com>"

# Admin email to receive leads
ADMIN_EMAIL=mkt@ateneointeriores.com.br

# Google Apps Script URL (optional)
GOOGLE_APPS_SCRIPT_URL="https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec"

# If frontend runs on a different host, set base URL
# NEXT_PUBLIC_API_BASE_URL=https://seu-dominio.com/api
# VITE_API_BASE_URL=https://seu-dominio.com/api
